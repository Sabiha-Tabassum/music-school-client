import React from 'react';

const SectionTitle = ({heading}) => {
    return (
        <div className='text-center my-20 font-semibold text-4xl text-indigo-500 '>
            {heading}
        </div>
    );
};

export default SectionTitle;